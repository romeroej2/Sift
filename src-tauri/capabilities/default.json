{
  "identifier": "default",
  "description": "Default capability for the SIFT main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "notification:default"
  ]
}
